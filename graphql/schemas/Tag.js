const types = `
    type Tag {
        id : Int!
        text : String!
    }
    
    input TagInput {
        text : String!
    }
`

const queries = `
    
`

const mutations = `
    
`

module.exports = {types, queries, mutations}